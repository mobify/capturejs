machine:
    node:
        version: 0.10.22

dependencies:
    pre:
        - npm install -g grunt-cli bower
    override:
        - npm install
        - bower install

test:
    override:
        - grunt test
